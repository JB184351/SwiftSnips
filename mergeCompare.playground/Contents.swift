import Cocoa

// array of integers to be sorted into two list
var a: [Int] = [1, 4, 2, 7, 3, 4, 10, 8]
var n = a.count
// two empty arrays to sort the array into for later

var b: [Int] = []
var c: [Int] = []

b = a[0..<n - 1]




